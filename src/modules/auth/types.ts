export type SignInBody = {
    email: string;
    password: string;
}

export type SignUpBody = {
    firstName: string;
    lastName: string;
    dateOfBirth: string;
    email: string;
    password: string;
}
